# Snapshot — Signed Photo (docs-as-code)

ONE-DOC: функциональные требования (RU), PlantUML через Kroki.

## Локальный запуск

```bash
python3 -m venv .venv && source .venv/bin/activate
pip install -U pip && pip install -r requirements.txt
mkdocs serve
```
